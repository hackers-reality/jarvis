import React from "react";

export function UserProfilePanel() {
  return (
    <div style={{
      padding: "20px",
      background: "var(--j-surface)",
      border: "1px solid var(--j-border)",
      borderRadius: "8px",
      color: "var(--j-text-muted)",
      fontSize: "13px",
      lineHeight: 1.6,
    }}>
      User profile controls are available after merging the latest settings updates from main.
    </div>
  );
}
